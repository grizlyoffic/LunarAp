buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

// allprojects block yahan se hata dein

task clean(type: Delete) {
    delete rootProject.buildDir
}
